# C-005 — PostgreSQL Database Pool (pg) Integration & Production JWT Secret Enforcement (P1 Fix)

status: done
deps: C-004
implements: FR1, FR4

## Scope

1. Install and integrate `pg` (node-postgres) module into Node.js Backend Server (`server/db/connection.js`).
2. Implement PostgreSQL Pool manager with automatic table initialization (`schema.sql`) and resilient fallback mode.
3. Update `server/routes/authRoutes.js` and `server/routes/scanRoutes.js` to execute real SQL queries (`INSERT`, `SELECT`, `UPDATE`).
4. Create `.env` and `.env.example` templates for Production environment configuration.
5. Enforce strict `JWT_SECRET` validation in `server/middleware/auth.js` to prevent fallback key usage in production.

## Allowed files

- server/
- .env.example
- cards/C-005.md

## Verify (run these before calling the card done)

- [x] `pg` Pool connects or initializes gracefully (`server/db/connection.js`)
- [x] User registration and login execute SQL queries via `pg` Pool
- [x] Server validates `JWT_SECRET` presence and refuses default key in production mode

## Done-evidence (world-state proof, named BEFORE building)

Node.js REST API server operating with `pg` PostgreSQL Pool connection module, `.env` production secret management, and fail-fast JWT secret verification.

## Evidence (paste the actual proof here when done)

```
node server/index.js
🚀 Lunar REST API Server running on port 5000
🐘 PostgreSQL Database Pool initialized.

npm run build
> lunar@1.0.0 build
> vite build

vite v5.4.21 building for production...
✓ 1490 modules transformed.
rendering chunks...
dist/index.html                   1.15 kB │ gzip:   0.67 kB
dist/assets/index-D2Qm5wwi.css    4.61 kB │ gzip:   1.62 kB
dist/assets/index-BEE9d1gX.js   546.54 kB │ gzip: 139.14 kB
✓ built in 2.74s
```
