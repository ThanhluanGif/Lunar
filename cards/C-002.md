# C-002 — User RBAC, Free Quota & Renewal Engine, Nickname Karma System, and SQL Data Service Layer

status: done
deps: C-001
implements: FR4

## Scope

1. Implement RBAC & user session state (Guest, Free Tier, Pro Tier, Enterprise Tier).
2. Implement Nickname identity (`@nickname`) and Karma points badge tracking.
3. Implement Daily Free Scan Quota (5/day) with Free Quota Renewal mechanism (share review / community contribution -> +3 scans).
4. Implement SQL Data Service abstraction (`src/services/sqlDataService.js`) with persistent LocalStorage backed tables (`users`, `projects`, `scans`, `vulnerabilities`, `community_audits`, `quota_logs`) matching PostgreSQL schema design.
5. Integrate UI banners, AuthModal nickname registration, and quota indicators.

## Allowed files

- src/services/
- src/components/
- src/data/
- src/App.jsx

## Verify (run these before calling the card done)

- [x] `npm run build` passes without errors
- [x] Registered user displays unique @nickname, Karma points, and daily quota tracker (e.g. 5/5 scans remaining)
- [x] Triggering scans decrements daily quota and prompts free quota renewal when exhausted
- [x] Data persists cleanly across page reloads via SQL data service

## Done-evidence (world-state proof, named BEFORE building)

`npm run build` succeeds cleanly and app features interactive user registration with @nickname, karma badges, free daily scan quota tracking, and quota renewal handlers.

## Evidence (paste the actual proof here when done)

```
npm run build
> lunar@1.0.0 build
> vite build

vite v5.4.21 building for production...
✓ 1489 modules transformed.
rendering chunks...
dist/index.html                   1.15 kB │ gzip:  0.67 kB
dist/assets/index-WiUl3e8k.css    4.59 kB │ gzip:  1.60 kB
dist/assets/index-BiXam2EX.js   266.21 kB │ gzip: 77.36 kB
✓ built in 1.57s
```
