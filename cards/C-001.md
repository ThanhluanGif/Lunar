# C-001 — Khóa production API edge reliability contract từ source tới canonical Vercel

status: todo
deps: none
implements: FR1

## Scope

Xây đúng một vertical slice **Production API edge reliability contract**: khóa Vercel API rewrite/serverless entry trước SPA fallback bằng regression hành vi; thay parser non-JSON bằng pure header-only classifier đúng bốn 403 cases và không đọc/log HTML; cung cấp một live-smoke command read-only chỉ chấp nhận canonical production origin và xuất report theo `flow/05-contract.md`. Không đổi business endpoint/schema, auth/CORS/cookie/database, UI feature hoặc security control.

## Allowed files

- `vercel.json`
- `api/index.js`
- `src/services/lunarApi.js`
- `src/services/apiResponseError.js` (new)
- `scripts/production-routing-regression.mjs`
- `scripts/production-routing-live.mjs` (new)
- `package.json`
- `cards/C-001.md`

## Verify (run these before calling the card done)

- [ ] Red proof: với API SPA fallback tạm thời đổi lại thành `/:path*`, `npm run qa:production-routing` FAIL vì route contract sai; restore fix rồi cùng lệnh PASS.
- [ ] Red proof: với nhánh `x-vercel-mitigated` tạm thời bỏ khỏi pure classifier, `npm run qa:production-routing` FAIL vì expected `VERCEL_EDGE_FORBIDDEN` nhưng nhận `HOSTING_FORBIDDEN`; restore fix rồi cùng lệnh PASS 4/4 synthetic cases.
- [ ] `npm run qa:production-routing` exit 0 và report PASS cho API rewrite order, serverless entry, 4/4 classifier cases, CORS/public URL/cookie regression hiện hữu.
- [ ] `npm run qa:production-routing:browser` exit 0, browser gọi đúng API origin và credentialed CORS origin.
- [ ] `npm run build` exit 0.
- [ ] `npm run qa:production-routing:live -- --origin https://lunar-zeta-ruddy.vercel.app` exit 0, stdout `status=PASS`, `summary={passed:3,total:3}`, không chứa response body/challenge token/secret.
- [ ] `FLOW_SESSION_ID=codex-waf-403 bash /Users/admin/.agents/skills/flow/runner/flow.sh check C-001` exit 0 sau khi evidence thật được dán.

## Done-evidence (world-state proof, named BEFORE building)

Deployment chứa card commit được gắn vào canonical production alias `https://lunar-zeta-ruddy.vercel.app`; live-smoke từ máy operator chứng minh 3/3: health 200 JSON, unknown `/api/v1` 404 JSON (không phải SPA HTML), và login preflight 204/CORS đúng origin. Evidence phải ghi deployment URL/SHA hoặc Vercel deployment ID cùng report đã redact; test output riêng không thay thế bằng chứng live này.

## Evidence (paste the actual proof here when done)

(empty until done)
