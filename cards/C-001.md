# C-001 — Xây cổng xác minh GitHub OAuth và độ tin cậy của scan

status: todo
deps: none
implements: FR1 FR2

## Scope

Xây đúng một **Core trust verification gate** không sửa application behavior: một corpus 40 mẫu
JavaScript có ground truth; lệnh baseline đo riêng deterministic SAST và ba lần AI review; một
interactive Puppeteer flow để operator hoàn tất GitHub consent trên canonical production rồi kiểm
năm checkpoint; và một report đã redact với verdict PASS/FAIL/BLOCKED cùng lệnh enforce threshold.
Baseline thất bại vẫn là evidence hợp lệ; C-001 không được sửa OAuth, scanner hoặc prompt để làm số
đẹp lên.

## Allowed files

- `tests/fixtures/security-benchmark.json` (new)
- `scripts/core-trust-metrics.mjs` (new)
- `scripts/core-trust-baseline.mjs` (new)
- `scripts/core-trust-live.mjs` (new)
- `scripts/core-trust-enforce.mjs` (new)
- `scripts/core-trust-regression.mjs` (new)
- `package.json`
- `cards/C-001.md`

## Verify (run these before calling the card done)

- [ ] Fixture audit: exactly 40 unique cases; `CWE-78`, `CWE-79`, `CWE-89`, `CWE-95` and
  `CWE-798` each contain 4 VULNERABLE + 4 SAFE cases;
  labels were manually reviewed and are not generated from Lunar output.
- [ ] Red proof: deterministic baseline classifies current `exec(req.query.command)` case as FN;
  `npm run qa:core-trust:enforce -- --report .flow/core-trust-baseline.json` exits 1 while the
  baseline command exits 0
  with verdict FAIL, proving measurement is not laundered into release success.
- [ ] `npm run qa:core-trust:regression` exits 0 and covers metric formulas, case normalization,
  5-checkpoint completeness, PASS/FAIL/BLOCKED precedence, invalid input exit 2 and redaction.
- [ ] `npm run qa:core-trust:baseline -- --mode deterministic` emits a schema-valid report for
  40/40 cases with TP/TN/FP/FN, precision, recall and criticalRecall; no source or secret field.
- [ ] `npm run qa:core-trust:baseline -- --mode live --origin https://lunar-zeta-ruddy.vercel.app`
  opens an interactive browser; operator alone authorizes the dedicated GitHub test account; report
  records all 5 OAuth checkpoints and exactly 3 AI runs or a stable BLOCKED reason.
- [ ] Live report contains 0 cookie/JWT/OAuth state/code/token/email/login/repository name or URL,
  raw source, raw prompt or raw AI response; temporary browser profile is removed after close.
- [ ] `npm run build` exits 0 and existing `npm run qa:production-routing` exits 0.
- [ ] `FLOW_SESSION_ID=codex-core-trust bash /Users/admin/.agents/skills/flow/runner/flow.sh check C-001`
  exits 0 only after actual evidence is pasted.

## Done-evidence (world-state proof, named BEFORE building)

One timestamped, redacted `CoreTrustBaselineReport` against
`https://lunar-zeta-ruddy.vercel.app` records OAuth 5/5 checkpoints and deterministic 40/40 plus
AI 3/3 measurements (or a precise BLOCKED provider/quota reason), with `secretFieldsEmitted=0`.
Evidence includes the report hash, corpus version, command exit codes and explicit release verdict;
it may honestly be FAIL/BLOCKED. A green test without the interactive production trace and measured
confusion matrices is not done.

## Evidence (paste the actual proof here when done)

(empty until done)
