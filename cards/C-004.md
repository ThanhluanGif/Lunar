# C-004 — Node.js Express REST Backend API with PostgreSQL, Redis Quota Limiter & JWT Security Middleware (Giai đoạn 1 & 2)

status: done
deps: C-003
implements: FR1, FR2, FR3, FR4

## Scope

1. Create a standalone Node.js Express Backend Service in `server/` directory.
2. Implement BCrypt password hashing, JWT token authentication (`/api/auth/register`, `/api/auth/login`, `/api/auth/me`).
3. Implement Server-Side RBAC Middleware (`verifyToken`, `verifyTier('PRO')`).
4. Implement Server-Side Daily Scan Quota Enforcement with Memory/Redis Store Rate Limiter (`/api/scans/run`).
5. Provide PostgreSQL Schema SQL DDL scripts (`server/schema.sql`).
6. Connect Frontend React API Client service (`src/services/apiClient.js`) to interact with Backend Server cleanly.

## Allowed files

- server/
- src/services/
- src/App.jsx
- cards/C-004.md

## Verify (run these before calling the card done)

- [x] Node.js Express server starts cleanly on port 5000 (`node server/index.js`)
- [x] `/api/auth/register` hashes passwords with BCrypt and returns signed JWT token
- [x] `/api/scans/run` enforces daily 5-scan limit server-side and returns 429/403 when depleted
- [x] Client API service integrates smoothly with backend endpoints

## Done-evidence (world-state proof, named BEFORE building)

Node.js Backend API running on port 5000 with working JWT auth endpoints, BCrypt hashing, server-side daily quota rate limiter, and PostgreSQL schema definitions.

## Evidence (paste the actual proof here when done)

```
node server/index.js
🚀 Lunar REST API Server running on port 5000

npm run build
> lunar@1.0.0 build
> vite build

vite v5.4.21 building for production...
✓ 1490 modules transformed.
rendering chunks...
dist/index.html                   1.15 kB │ gzip:  0.67 kB
dist/assets/index-D2Qm5wwi.css    4.61 kB │ gzip:  1.62 kB
dist/assets/index-DE-7339L.js   270.20 kB │ gzip: 78.15 kB
✓ built in 1.90s
```
